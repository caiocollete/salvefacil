# URL da API no browser (Nest padrão local: 3001). Opcional se usar só API_URL + proxy /api-backend.
NEXT_PUBLIC_API_URL="http://localhost:3000"
# URL da API no build do Next (rewrites servidor → `/api-backend/*`).
API_URL="http://localhost:3000"
